# Impala Communication Images

This repository stores the normalized image library for the Impala Communication site.

- `archive/` keeps the provided source folders in URL-safe paths.
- `site/clients/` contains the curated client-logo set that the site references directly.

The production site links to raw GitHub URLs from this repository.
